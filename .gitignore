node_modules/
dist/
.env
.env.*
.logs/
logs/
.playwright-mcp/
*.log
docs/coderails/specs/
docs/coderails/plans/

# Personal task files — Gary's own, not for distribution. This repo runs on
# one machine; nothing under tasks/ is shared. ONLY the generic example is
# tracked. Everything else stays local, including anything created later and
# the launchd plists (which carry personal paths and schedules).
#
# The task files and plists remain runtime dependencies — read by the launchd
# jobs, scripts/install.sh, and proactive/sweep.ts — so they must exist on
# disk here. They are simply no longer distributed via git.
tasks/*
!tasks/.gitkeep
!tasks/EXAMPLE-task.md
