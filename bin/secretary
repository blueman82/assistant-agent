#!/usr/bin/env zsh
# Launch Gary's secretary from anywhere.
# Resolves its own location (following symlinks via zsh's ${0:A}), so the
# repo can live anywhere and ~/bin/secretary can symlink here.
REPO="${0:A:h:h}"
exec "$REPO/node_modules/.bin/tsx" "$REPO/secretary.ts" "$@"
